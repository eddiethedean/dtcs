dtcsVersion: "1.0.0"
id: "expression.whitespace.type"
name: "Whitespace Expression Invalid Type"
version: "0.1.0"

inputs:
  - id: "in"
    schema:
      fields:
        - name: "value"
          type: "integer"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "integer"
          nullable: false

expressions:
  - id: "calc"
    type: "not-a-type"
    expr: "   "

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
