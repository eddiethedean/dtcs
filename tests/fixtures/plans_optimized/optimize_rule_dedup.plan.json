{
  "identity": {
    "dtcsVersion": "1.0.0",
    "id": "optimize.rule.dedup",
    "name": "Optimize rule dedup",
    "version": "0.1.0"
  },
  "inputs": [
    {
      "id": "in",
      "schema": {
        "fields": [
          {
            "name": "customer_id",
            "type": "string",
            "nullable": false
          }
        ]
      },
      "optional": false
    }
  ],
  "outputs": [
    {
      "id": "out",
      "schema": {
        "fields": [
          {
            "name": "customer_id",
            "type": "string",
            "nullable": false
          }
        ]
      }
    }
  ],
  "nodes": [
    {
      "id": "customer_id_required_a",
      "kind": {
        "kind": "rule",
        "id": "customer_id_required_a",
        "rule": "dtcs:not_null",
        "target": "in.customer_id",
        "phase": "postcondition"
      },
      "objectRef": "rules.customer_id_required_a"
    }
  ],
  "dependencies": [
    {
      "from": "in",
      "to": "customer_id_required_a",
      "reason": "fieldRead"
    },
    {
      "from": "in",
      "to": "out",
      "reason": "lineage"
    }
  ],
  "lineage": {
    "mappings": [
      {
        "output": "out",
        "inputs": [
          "in"
        ],
        "operation": "dtcs:derive",
        "flow": "derived"
      }
    ]
  },
  "guarantees": {}
}
