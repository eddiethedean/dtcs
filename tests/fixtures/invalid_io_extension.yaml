dtcsVersion: "1.0.0"
id: "bad.io.extension"
name: "Bad IO Extension"
version: "0.1.0"

inputs:
  - id: "in"
    description: "misplaced description"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
