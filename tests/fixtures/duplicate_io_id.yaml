dtcsVersion: "1.0.0"
id: "duplicate.io.id"
name: "Duplicate IO ID"
version: "0.1.0"

inputs:
  - id: "shared"
    schema:
      fields:
        - name: "value"
          type: "integer"
          nullable: false

outputs:
  - id: "shared"
    schema:
      fields:
        - name: "value"
          type: "integer"
          nullable: false

lineage:
  mappings:
    - output: "shared"
      inputs: ["shared"]
