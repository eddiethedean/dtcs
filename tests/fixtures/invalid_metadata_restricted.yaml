dtcsVersion: "1.0.0"
id: "restricted.example"
name: "Restricted Example"
version: "0.1.0"

metadata:
  classification: restricted
  governance: {}

inputs:
  - id: "in"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
