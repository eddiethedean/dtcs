{
  "identity": {
    "dtcsVersion": "1.0.0",
    "id": "stdlib.action.trim.valid",
    "name": "Stdlib Trim Valid",
    "version": "0.1.0"
  },
  "inputs": [
    {
      "id": "in",
      "schema": {
        "fields": [
          {
            "name": "text",
            "type": "string",
            "nullable": true
          }
        ]
      },
      "optional": false
    }
  ],
  "outputs": [
    {
      "id": "out",
      "schema": {
        "fields": [
          {
            "name": "text_trim",
            "type": "string",
            "nullable": true
          }
        ]
      }
    }
  ],
  "nodes": [
    {
      "id": "trim_text",
      "kind": {
        "kind": "semanticAction",
        "id": "trim_text",
        "action": "dtcs:trim",
        "target": "in.text"
      },
      "objectRef": "semanticActions.trim_text"
    }
  ],
  "dependencies": [
    {
      "from": "in",
      "to": "out",
      "reason": "lineage"
    },
    {
      "from": "in",
      "to": "trim_text",
      "reason": "fieldRead"
    }
  ],
  "lineage": {
    "mappings": [
      {
        "output": "out",
        "inputs": [
          "in"
        ],
        "operation": "dtcs:derive",
        "flow": "derived"
      }
    ]
  },
  "guarantees": {},
  "findings": [
    {
      "objectRef": "semanticActions.trim_text",
      "kind": "lineageCoverage",
      "message": "semantic action targets 'in', but lineage mappings do not mention that output"
    }
  ]
}
