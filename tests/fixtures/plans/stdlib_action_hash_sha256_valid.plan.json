{
  "identity": {
    "dtcsVersion": "1.0.0",
    "id": "stdlib.action.hash_sha256.valid",
    "name": "Stdlib Hash SHA256 Valid",
    "version": "0.1.0"
  },
  "inputs": [
    {
      "id": "in",
      "schema": {
        "fields": [
          {
            "name": "secret",
            "type": "string",
            "nullable": true
          }
        ]
      },
      "optional": false
    }
  ],
  "outputs": [
    {
      "id": "out",
      "schema": {
        "fields": [
          {
            "name": "digest",
            "type": "string",
            "nullable": false
          }
        ]
      }
    }
  ],
  "nodes": [
    {
      "id": "hash",
      "kind": {
        "kind": "semanticAction",
        "id": "hash",
        "action": "dtcs:hash_sha256",
        "target": "in.secret"
      },
      "objectRef": "semanticActions.hash"
    }
  ],
  "dependencies": [
    {
      "from": "in",
      "to": "hash",
      "reason": "fieldRead"
    },
    {
      "from": "in",
      "to": "out",
      "reason": "lineage"
    }
  ],
  "lineage": {
    "mappings": [
      {
        "output": "out",
        "inputs": [
          "in"
        ]
      }
    ]
  },
  "guarantees": {},
  "findings": [
    {
      "objectRef": "semanticActions.hash",
      "kind": "lineageCoverage",
      "message": "semantic action targets 'in', but lineage mappings do not mention that output"
    }
  ]
}
