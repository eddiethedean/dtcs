{
  "identity": {
    "dtcsVersion": "1.0.0",
    "id": "stdlib.action.capitalize.valid",
    "name": "Stdlib Capitalize Valid",
    "version": "0.1.0"
  },
  "inputs": [
    {
      "id": "in",
      "schema": {
        "fields": [
          {
            "name": "name",
            "type": "string",
            "nullable": true
          }
        ]
      },
      "optional": false
    }
  ],
  "outputs": [
    {
      "id": "out",
      "schema": {
        "fields": [
          {
            "name": "name_cap",
            "type": "string",
            "nullable": true
          }
        ]
      }
    }
  ],
  "nodes": [
    {
      "id": "cap",
      "kind": {
        "kind": "semanticAction",
        "id": "cap",
        "action": "dtcs:capitalize",
        "target": "in.name"
      },
      "objectRef": "semanticActions.cap"
    }
  ],
  "dependencies": [
    {
      "from": "in",
      "to": "cap",
      "reason": "fieldRead"
    },
    {
      "from": "in",
      "to": "out",
      "reason": "lineage"
    }
  ],
  "lineage": {
    "mappings": [
      {
        "output": "out",
        "inputs": [
          "in"
        ],
        "operation": "dtcs:derive",
        "flow": "derived"
      }
    ]
  },
  "guarantees": {},
  "findings": [
    {
      "objectRef": "semanticActions.cap",
      "kind": "lineageCoverage",
      "message": "semantic action targets 'in', but lineage mappings do not mention that output"
    }
  ]
}
