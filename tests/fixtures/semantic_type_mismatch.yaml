dtcsVersion: "1.0.0"
id: "broken"
name: "Broken"
version: "0.1.0"
inputs:
  - id: "in"
    schema:
      fields:
        - name: "value"
          type: "integer"
          nullable: false
outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false
lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
semanticActions:
  - id: "bad_type"
    action: "dtcs:lowercase"
    target: "in.value"
