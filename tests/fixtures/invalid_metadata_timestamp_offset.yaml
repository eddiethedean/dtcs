dtcsVersion: "1.0.0"
id: "bad.timestamp.offset"
name: "Bad Timestamp Offset"
version: "0.1.0"

metadata:
  provenance:
    createdAt: "2026-01-01T00:00:00+99:99"

inputs:
  - id: "in"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
