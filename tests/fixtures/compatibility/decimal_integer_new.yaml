dtcsVersion: "1.0.0"
id: "compat.decimal"
name: "Decimal Integer New"
version: "1.1.0"

inputs:
  - id: "in"
    schema:
      fields:
        - name: "amount"
          type: "integer"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "amount"
          type: "integer"
          nullable: false

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
