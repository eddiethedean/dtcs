dtcsVersion: "1.0.0"
id: "compat.streaming"
name: "Streaming New"
version: "1.1.0"

inputs:
  - id: "in"
    streaming:
      mode: bounded
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
