dtcsVersion: "1.0.0"
id: "broken"
name: "Broken"
version: "0.1.0"
inputs:
  - id: "in"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: true
outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false
lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
rules:
  - id: "bad_rule"
    rule: "dtcs:unknown_rule"
    target: "value"
    phase: "postcondition"
