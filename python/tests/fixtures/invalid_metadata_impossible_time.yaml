dtcsVersion: "1.0.0"
id: "invalid.metadata.time"
name: "Invalid Metadata Time"
version: "0.1.0"

metadata:
  provenance:
    createdAt: "2026-01-15T99:99:99Z"

inputs:
  - id: "in"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

outputs:
  - id: "out"
    schema:
      fields:
        - name: "value"
          type: "string"
          nullable: false

lineage:
  mappings:
    - output: "out"
      inputs: ["in"]
